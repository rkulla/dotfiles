Install the following for my minimum Javascript dependencies:

    # eslint
    $ npm i eslint-config-airbnb-base -D
    $ npx install-peerdeps --dev eslint-config-airbnb-base
    $ npm i babel-preset-airbnb -D 
